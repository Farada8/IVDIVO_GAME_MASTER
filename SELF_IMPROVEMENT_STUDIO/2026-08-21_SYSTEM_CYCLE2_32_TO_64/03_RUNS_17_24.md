# CYCLE 2 — RUNS 17–24

## RUN 17 — TOOL-CALL GUARDRAIL DESIGNER
**Status:** `PASS_ENGINEERING`. Prompt-only safety is insufficient for stale writes, wrong authority or irreversible actions. Created pre/post mutation guardrails: freshness read, authority/approval check, expected target hash, reversibility/idempotence, secret scan, expected diff, readback, schema/regression, rollback. **Artifact:** `TOOL_CALL_GUARDRAILS_v0.1.json`.

## RUN 18 — MULTI-AI RECONCILER
**Status:** `PASS_ENGINEERING`. Created schema separating diagnosis validity from repair validity, collapsing duplicate evidence families and dispositioning `ACCEPT / ACCEPT_WITH_MODIFICATION / HOLD_FOR_TEST / REJECT`. **Artifact:** `MULTI_AI_RECONCILER_SCHEMA_v0.1.json`. Multiple phrasings of one unsupported assumption count as one evidence family, not votes.

## RUN 19 — ENGINE EXTENSION VS NEW BUILD GATE
**Status:** `PASS_EXTEND_CURRENT`. Required orchestration modules fit as extensions to Self-Improvement v2 + current machine execution. A new top-level engine would create parallel authority and migration debt. **Artifact:** `ENGINE_EXTENSION_VS_NEW_GATE_v0.1.json`. Decision: `EXTEND_CURRENT_COMPATIBILITY_LAYER`.

## RUN 20 — ENGINE STATE/DAG COMPILER
**Status:** `PASS_ENGINEERING`. Defined typed execution DAG: `ADAPT_STATE → NORMALIZE_FRONTIER → ROUTE → COMPILE_PROMPT_IR → PRE_GUARDS → EXECUTE → POST_GUARDS → VERIFY → PERSIST_TRANSACTION → TELEMETRY → LEARN → REBASE → ROUTE_AGAIN`, with explicit stop states. **Artifact:** `META_ORCHESTRATOR_DAG_SCHEMA_v0.2.json`.

## RUN 21 — TRANSACTIONAL WRITE-THROUGH ENGINE
**Status:** `PASS_ENGINEERING`. Implemented bounded transaction helper with expected-hash freshness token, no-effect rejection, apply/readback verifier and rollback. Deterministic fixtures cover stale write, no-effect, rollback and commit. **Artifact:** `transactional_write_through_v0_1.py`. Concurrent dialogs make this mandatory, not theoretical.

## RUN 22 — REGISTRY TRANSACTION API
**Status:** `PASS_ENGINEERING`. Implemented candidate lifecycle validation: provenance, owner, next action/gate, application targets for promoted/applied states, verification evidence for `VERIFIED_CURRENT`, HOLD trigger, duplicate-ID protection. **Artifact:** `registry_transaction_api_v0_1.py`. **Next:** use when merging SI-0008/this cycle into central registry after branch integration.

## RUN 23 — CROSS-DOMAIN EVIDENCE MAP
**Status:** `PASS`. Mapped claim classes to sufficient evidence and listed what lower evidence cannot prove: code correctness, literary quality, audio role fit, provider capability, market behavior and workflow durability. **Artifact:** `CROSS_DOMAIN_EVIDENCE_MAP_v0.1.json`. **Law:** evidence must match the claim, not merely exist.

## RUN 24 — ONE-VARIABLE CANARY DESIGNER
**Status:** `PASS`. Created canary schema with baseline, one treatment, precommitted thresholds, confounds, cost cap and rollback. **Artifact:** `ONE_VARIABLE_CANARY_SCHEMA_v0.1.json`. **Next:** Prompt IR migration and one audio provider/casting experiment.

## Cross-run learning
The execution engine should be extended, not replaced. Mutating actions require guardrails around the tool call plus transactional persistence. Evaluation must record claim-specific evidence; self-improvement experiments should change few variables at a time so results remain attributable.