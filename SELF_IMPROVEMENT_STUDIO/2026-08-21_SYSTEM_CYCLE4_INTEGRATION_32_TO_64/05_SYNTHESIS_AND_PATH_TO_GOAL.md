# CYCLE 4 — INTEGRATED SYNTHESIS + PATH TO GOAL

Cycle 4 moved SI-0012 from a small routing compatibility pilot toward a bounded **system-integration spine**. The advance is not prompt quantity; it is machine-enforced separation of authority, work frontier, evidence, scope, mutation state and learning.

## 12 conclusions
1. **Authority != newest work.** A fresher PR/Drive frontier may be useful without becoming authority; THE WIFE PR #85 is the key fixture.
2. **Supersession must be scope-aware.** Story completion, Founder Lock, human listen, provider casting and release are different axes.
3. **Readiness is a vector, not one word `LOCKED`.**
4. **Shared facts need one lineage.** Exact clue/consent/identity facts must not be independently regenerated downstream.
5. **Agent agreement is not independent evidence.** Collapse derived/duplicate reports by provenance family.
6. **Prompt IR is valid only if control semantics survive rehydration.**
7. **Context compaction is dependency-aware.** Shorter context is unsafe if authority/protected facts/gates disappear.
8. **Writes are transactions, not optimistic prose claims.** Expected hash/revision, scope, secret scan, readback and repair state are mandatory.
9. **Telemetry preserves unknowns.** `null` beats invented zero.
10. **Economics is an evidence contract.** Without measured spend/time/accepted minutes, status is HOLD.
11. **Package identity is immutable.** Post-v11.2 improvements require a new package build; the old ZIP does not drift.
12. **Self-improvement needs a governor.** If book/audio/human/provider evidence has higher information value, reject ritual prompt proliferation.

## Engineering result
- 32/32 executed;
- 31 PASS_ENGINEERING;
- 1 PASS_CONTRACT_HOLD_REAL_DATA;
- 100/100 warm regression PASS;
- 100/100 cold-unpack PASS;
- 16 new integration modules; 12 new contracts;
- 64 Cycle-5 prompts derived / NOT AUTO-AUTHORIZED.

## P1 books
- D09: Founder Lock decision; no E25.
- D10 BLOODBOUND: Founder Lock decision; no more prose.
- THE WIFE: freshness/rebase PR #85; if still valid, E96 is next story production.
- Orbital Youth: external-feedback ready; no speculative broad rewrite.

## P2 audio
- D04: real G4 machine prototype exists; next decisive evidence is blind human A/B response. Provider/casting is a separate gate.
- ROOM917: use current post-render state; do not return to S0/S1.
- Universal Audio Novel Engine: converge genuine PR82/84/86 deltas into one `audio/studio/runtime`; no second runtime.
- LESSON ZERO live canary remains authenticated-provider/cast dependent.

## P3/P4 factory
1. land only non-duplicated SI-0012 contracts;
2. wire telemetry to real project events;
3. rehearse deterministic registry-family compaction;
4. run two-model/provider-adapter parity when a second execution surface exists;
5. build a new engine package only after accepted post-v11.2 modules are dependency-closed and fully regressed.

## Post-execution concurrent confirmation
Fresh main recorded ROOM917 SI-0009 learning after the 32-run cycle. Independent result: documentation is not operational completeness. A reusable law needs executable enforcement, versioned interfaces, real pilot, regression and independent second-project replication before domain promotion.

`WRITTEN LAW -> EXECUTABLE ENFORCEMENT -> VERSIONED INTERFACE -> REAL PILOT -> REGRESSION -> SECOND-PROJECT REPLICATION -> PROMOTION`.

## Stop law
`PRODUCTION -> OBSERVE -> ROOT CAUSE -> SMALLEST DECISIVE PILOT -> EVIDENCE -> APPLY -> VERIFY -> LEARN`.

Do not replace it with `MORE PROMPTS -> MORE PROMPTS`.