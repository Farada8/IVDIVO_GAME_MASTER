# D01 POST-LOCK AUDIO — FRESH PORT 2026-08-22

## Authority

- Story authority remains the Founder-locked D01 E01–E120 package.
- E121 is prohibited absent a valid reopen trigger.
- This change does not mutate story text.

## Freshness

Fresh branch cut from main commit `669677e013cdde795d09ddf0e3a0f51a2ceabe5a`.

The production `audio/studio/runtime/cast_readiness.py` at that main commit was still hard-coded to Lesson Zero roles `NARRATOR/ETHAN/AOIFE`, pronunciation terms `Ифа/Контакт`, and pair `ETHAN/AOIFE`.

PR #168 contained a useful but stale project-cast extension. This port deliberately reuses only its unique bounded delta:

1. backward-compatible project-specific roles;
2. project pronunciation terms;
3. project pair gate;
4. project fatigue window;
5. fail-closed cast-spec validation;
6. D01 regression coverage;
7. D01 E01 cast-spec fixture.

Stale state/router/prose files from PR #168 are not copied.

## D01 cast scope

Required initial roles:
- NARRATOR
- MARA
- ADRIAN
- LILY
- CELESTE

Primary pair gate: `MARA / ADRIAN`.

Supporting `OFFICIANT` and `SECURITY` remain deferred for the bounded E01 casting stage.

## Safety invariants

Before real provider evidence and human audition:

- `provider_dispatch_allowed = false`
- `machine_may_auto_lock = false`
- `voice_lock = false`
- no automatic voice substitution
- provider metadata is not Human Signal
- no paid synthesis is authorized by this port

Lesson Zero callers remain semantically backward-compatible when project parameters are omitted.

## Evidence ceiling

This port claims only code/spec/test preparation.

Not claimed:
- real AUTH_PROVIDER snapshot;
- verified current ElevenLabs voice inventory;
- real D01 voice candidates;
- human auditions;
- voice locks;
- paid synthesis;
- live WAV;
- alignment/QC PASS;
- measured economics;
- Human Signal.

## External frontier

The existing `ElevenLabs Provider Snapshot Evidence` GitHub Actions workflow is the next empirical boundary. It is read-only and makes zero paid synthesis calls, but it requires `ELEVENLABS_API_KEY` as a repository secret configured outside chat/Git/Drive and a `workflow_dispatch` run.

The currently connected GitHub control surface can inspect and re-run existing workflows but cannot create a fresh `workflow_dispatch`. Therefore provider evidence remains `HOLD_EXTERNAL_PROVIDER_SNAPSHOT` until a real run artifact exists.

## Next causal chain

`AUTH_PROVIDER artifact -> normalized verified inventory -> D01 provisional candidate IDs -> human audition -> explicit voice lock -> zero-credit preflight -> bounded S01–S07 live smoke -> durable WAV/request/alignment provenance`
