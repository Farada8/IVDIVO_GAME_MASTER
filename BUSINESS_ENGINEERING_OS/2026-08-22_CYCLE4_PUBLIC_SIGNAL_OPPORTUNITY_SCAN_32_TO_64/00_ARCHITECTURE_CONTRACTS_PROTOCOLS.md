# BUSINESS ENGINEERING OS — CYCLE4 PUBLIC SIGNAL OPPORTUNITY ENGINE

**Status:** BOUNDED ENGINEERING / PUBLIC-SIGNAL EVIDENCE ONLY / NO_OUTREACH / €0 NEW FOUNDER CASH  
**Parent authority:** Cycle3 merged/closed on main.  
**Library authority:** 71 raw files; raw copyrighted binaries remain private Drive.

## Goal
Convert current Ireland/EU public signals into typed Opportunity Objects without fabricating demand, willingness-to-pay, economics or buyer commitments.

`LIBRARY_DELTA -> FRESH_PRIMARY_SIGNAL -> AUTHORITY/FRESHNESS -> DEADLINE/FORCED_ACTION -> BUYER_WORKLOAD -> MICRO_MARKET -> OPPORTUNITY -> FATAL_ASSUMPTION -> CHEAPEST_ZERO_CASH_NO_OUTREACH_TEST -> CREATE/BROKER/ACQUIRE -> K/S/E PROOF -> PORTFOLIO WIP -> LEARNING`

## Evidence planes
- K0–K5: knowledge/source proof.
- S0–S4: public signal proof.
- E0–E7: market/buyer proof.
- Public web research in this cycle may support S/K and at most E2-like public artefacts. It cannot create E3 interview evidence, E4 commitment/payment evidence, or profitability proof.

## Modules B81–B112
B81 `LibraryDeltaReconciler71` — composes prior full manifest with new Drive delta.  
B82 `RawSourcePrivacyGuard` — rejects raw copyrighted binary publication to public Git.  
B83 `FreshSignalCollector` — ingests dated public signals with source URL.  
B84 `PrimarySourceAuthorityResolver` — ranks EU/Irish official source above commentary.  
B85 `SignalFreshnessClock` — captures publication/event/application dates separately.  
B86 `SignalSupersessionResolver` — marks stale/superseded rules zero-current-weight.  
B87 `RegulatoryDeadlineCompiler` — compiles explicit applicable dates/windows.  
B88 `ForcedActionExtractor` — separates legal/operational obligation from optional trend.  
B89 `BuyerWorkloadCompilerV2` — translates signal into concrete recurring/manual work.  
B90 `OpportunityObjectCompilerV4` — emits typed opportunity without a magic total score.  
B91 `OpportunityDeduplicator` — merges opportunities sharing the same buyer/workflow root.  
B92 `SignalClusterer` — clusters related regulations without double evidence weighting.  
B93 `JurisdictionScopeRouter` — EU/IE/member-state scope and transposition state.  
B94 `MicroMarketCompilerV2` — chooses narrow first buyer segment and job-to-be-done.  
B95 `FatalAssumptionRankerV2` — finds earliest proposition that can kill the opportunity.  
B96 `NoOutreachExperimentRouter` — only public/no-contact tests in current cycle.  
B97 `PublicArtifactProbeEngine` — searches portals, manuals, forms, schemas, tenders and public job posts.  
B98 `PublicProcurementProbe` — detects public procurement evidence without treating it as private demand.  
B99 `GrantBridgeScanner` — detects buyer implementation support/grant pathways.  
B100 `ComplianceBurdenVector` — records frequency, complexity, evidence volume and deadline pressure separately.  
B101 `DeadlineUrgencyVector` — records proximity/recurrence, not a scalar desirability score.  
B102 `DataReadinessGate` — asks whether required source data exists in machine-usable form.  
B103 `WorkflowFragmentationDetector` — detects spreadsheet/email/portal/document fragmentation.  
B104 `AutomationSuitabilityGate` — separates deterministic/document-heavy work from expert-only judgment.  
B105 `EvidenceProductionSuitabilityGate` — tests whether output can be made auditable/reproducible.  
B106 `CreateBrokerAcquireRouterV4` — maintains separate CREATE/BROKER/ACQUIRE vectors.  
B107 `FounderCashGapRouterV3` — new founder cash is €0; unknown capital needs remain null.  
B108 `CustomerFundingTopologyProbe` — searches prepaid, implementation-funded, grant-backed or brokered routes.  
B109 `OutcomeProxyVerifier` — prohibits proxy metrics from becoming buyer outcome proof.  
B110 `DecisionDeltaLedger` — records what evidence changed which decision.  
B111 `PortfolioWIPGovernorV2` — caps simultaneous active experiments.  
B112 `SelfImprovementBusinessBridgeV2` — sends repeated engineering findings to SI as candidates only.

## Contracts C97–C128
C97 `LIBRARY_DELTA_RECONCILED_BEFORE_USE` — current file authority must be reconciled before new source claims.  
C98 `RAW_COPYRIGHTED_FILES_PRIVATE` — public Git never receives raw book binaries.  
C99 `SOURCE_PRESENT_NEQ_MECHANISM_PROVEN` — filename/metadata cannot establish a book mechanism.  
C100 `FRESH_SIGNAL_PRIMARY_SOURCE_REQUIRED` — CURRENT signal needs official/primary evidence where available.  
C101 `PUBLICATION_DATE_NEQ_EVENT_DATE_NEQ_APPLICATION_DATE` — dates remain separate.  
C102 `SUPERSEDED_SIGNAL_ZERO_CURRENT_WEIGHT` — outdated guidance cannot drive current action.  
C103 `SIGNAL_CLUSTER_ONE_EVIDENCE_WEIGHT` — correlated pages do not become independent confirmations.  
C104 `DEADLINE_NEQ_DEMAND` — deadline creates workload, not willingness-to-pay proof.  
C105 `FORCED_ACTION_NEQ_WTP` — legal obligation is not purchase commitment.  
C106 `BUYER_WORKLOAD_NEQ_BUYER_COMMITMENT` — inferred job is not interview/order evidence.  
C107 `PUBLIC_BUDGET_NEQ_VENDOR_BUDGET` — grants/budgets do not prove spend on our solution.  
C108 `UNKNOWN_ECONOMICS_NULL` — missing CAC, margin, price, conversion or cycle values remain null.  
C109 `NO_MAGIC_TOTAL_SCORE` — no weighted scalar may hide vetoes or unknowns.  
C110 `FATAL_ASSUMPTION_BEFORE_BUILD` — earliest kill condition precedes irreversible product build.  
C111 `CHEAPEST_DECISIVE_TEST` — select the lowest-cost test capable of changing the decision.  
C112 `ZERO_FOUNDER_CASH_TEST_REQUIRED` — current active test must require €0 new founder cash.  
C113 `NO_OUTREACH_CANNOT_CLAIM_E3` — no-contact research cannot become human buyer evidence.  
C114 `PUBLIC_ARTEFACT_NEQ_PRIVATE_PROCESS` — public form/manual may not represent actual internal workflow.  
C115 `JURISDICTION_SCOPE_EXPLICIT` — each signal/opportunity declares EU/IE/member-state applicability.  
C116 `TRANSPOSITION_GAP_NEQ_FINAL_NATIONAL_RULE` — EU directive plus Irish gap cannot be treated as enacted Irish detail.  
C117 `COMPLIANCE_NEQ_LEGAL_ADVICE` — engine scopes operational workload, not legal opinion.  
C118 `AUTOMATION_NEQ_ADJUDICATION` — AI may prepare evidence/workflow but does not make regulator/legal determinations.  
C119 `DATA_READINESS_BEFORE_AUTOMATION` — automate only after source-data path is identified.  
C120 `AUDIT_TRAIL_REQUIRED_FOR_COMPLIANCE_OUTPUT` — material compliance output binds source/version/time.  
C121 `CREATE_BROKER_ACQUIRE_SEPARATE` — vectors remain independent and may all be HOLD.  
C122 `CAPITAL_EXTERNAL_NEQ_FREE` — grants/customer finance/credit are capital, not zero cost.  
C123 `GRANT_ELIGIBILITY_NEQ_GRANT_AWARD` — a support programme is not financing proof.  
C124 `REPEATED_FAILURE_REVISES_HYPOTHESIS_OR_BOUNDARY` — do not repeat a failed probe unchanged.  
C125 `PORTFOLIO_WIP_BOUNDED` — only a small evidence-maximising set may become active experiments.  
C126 `DECISION_DELTA_REQUIRED` — research that changes no decision is logged as non-progress.  
C127 `SI_CANDIDATE_NO_AUTO_PROMOTION` — repeated mechanism is discovery/candidate until SI lifecycle gate.  
C128 `READBACK_AND_CI_BEFORE_CLOSURE` — cross-store persistence and executable proof required before cycle closure.

## Proof obligations P97–P128
P97 authority count composes 69+2=71.  
P98 raw-source policy rejects public binary path.  
P99 K1 source cannot emit promoted mechanism.  
P100 official primary source accepted.  
P101 three date fields remain distinguishable.  
P102 superseded signal becomes non-current.  
P103 correlated signal cluster counts once.  
P104 deadline signal cannot set `wtp_proven=true`.  
P105 forced action cannot set `buyer_commitment=true`.  
P106 inferred workload remains E<=2.  
P107 grant presence cannot set financing awarded.  
P108 missing economics serialize as null.  
P109 opportunity has vectors, no total score.  
P110 missing fatal assumption invalidates opportunity.  
P111 test selector prefers zero-cost decisive probe.  
P112 nonzero-founder-cash experiment blocked.  
P113 no-outreach mode blocks E3/E4.  
P114 public artefact marked proxy.  
P115 jurisdiction required.  
P116 transposition gap preserves uncertainty.  
P117 output includes `not_legal_advice=true`.  
P118 automation cannot adjudicate compliance.  
P119 absent data path blocks automation-ready state.  
P120 evidence output binds source URL/date.  
P121 CREATE/BROKER/ACQUIRE independently routed.  
P122 external capital never labelled free.  
P123 grant eligibility cannot become award.  
P124 repeated failed test changes hypothesis/boundary.  
P125 WIP cap enforced.  
P126 decision-delta zero is non-progress.  
P127 SI output remains candidate.  
P128 closure requires CI + Drive readback markers.

## Protocols
P-BIZ-09 `LibraryDeltaProtocol` — reconcile, dedupe, privacy, provenance.  
P-BIZ-10 `FreshSignalProtocol` — source authority, date triad, supersession, jurisdiction.  
P-BIZ-11 `ForcedWorkProtocol` — signal -> deadline -> forced action -> workload; no demand leap.  
P-BIZ-12 `OpportunityCompileProtocol` — workload -> micro-market -> assumptions -> independent vectors.  
P-BIZ-13 `ZeroCashExperimentProtocol` — public/no-contact decisive tests only.  
P-BIZ-14 `ComplianceAutomationBoundaryProtocol` — evidence production yes; legal adjudication no.  
P-BIZ-15 `PortfolioEvidenceProtocol` — WIP cap, decision delta, null economics.  
P-BIZ-16 `SelfImprovementPersistenceProtocol` — candidate-only SI bridge + CI/readback closure.

## Current cycle constraints
`NO_OUTREACH=true`; `NEW_FOUNDER_CASH_EUR=0`; `PUBLIC_EVIDENCE_CEILING=E2_PLUS`; `UNKNOWN_ECONOMICS=null`; `LEGAL_ADVICE=false`; `RAW_BOOKS_PUBLIC_GIT=false`.
