# BUSINESS ENGINEERING OS — LIBRARY

## Current authority
- Raw private Google Drive folder: `1X6mo94Qo103HheyDry4P3dcQkv5qZg6N`
- Current physical raw-file authority: **78 files** as of 2026-08-22.
- Current pointer: `CURRENT_LIBRARY_AUTHORITY_78_2026-08-22.md`.
- Previous full 69-file manifest: `../2026-08-21_CYCLE2_LIBRARY_NORMALIZATION_32_TO_64/01_LIBRARY_AUTHORITY_69.md`.
- `CURRENT_LIBRARY_AUTHORITY_71_2026-08-22.md` is retained as a historical partial concurrent reconciliation and is superseded as the CURRENT pointer.

## Reconciliation
The durable sequence is:
`69-file historical baseline -> partial concurrent 71-file pointer -> persisted Library78 delta + fresh RAW-folder enumeration -> current 78-file physical authority`.

The post-69 wave contains nine physical files, eight new unique valid byte hashes and one exact-byte duplicate (`The Mom Test`). The raw-file count is not a unique-work count; canonical edition/translation/format resolution remains `null` until completed.

## Storage policy
Raw copyrighted books/files remain in private Google Drive. This repository stores only derived metadata, source passports, hashes already measured, canonical aliases, mechanism claims, engineering contracts, tests and Drive provenance pointers.

## Identity law
`physical_file_id != byte_hash != edition_alias != canonical_work_id`.

Exact byte duplicates receive one evidence weight; editions, translations and formats retain separate provenance until canonical resolution.

## Evidence law
`SOURCE_PRESENT != MECHANISM_PROVEN != CURRENT_MARKET_FACT != BUYER_EVIDENCE`.

Knowledge K0-K5, public-signal S0-S4 and market E0-E7 proof are non-substitutable. Public policy, budget, tender or regulatory evidence may identify workload and market motion but cannot create E3 buyer interaction or E4 money/PO/deposit evidence.

## Concurrency law
A new CURRENT pointer is promoted only after fresh RAW-folder enumeration, persisted-delta reconciliation across GitHub/Drive/File Library, identity/dedup review and readback. Earlier pointers are retained as historical provenance rather than silently overwritten.
