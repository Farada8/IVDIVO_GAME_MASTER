# CYCLE7A — UNIQUE AUTHORITY / PROOF ENGINEERING

Parent Cycle7 PR #203 owns the generic readiness compiler. This file contains only the additive, non-conflicting extension.

## Modules C7A-M01–C7A-M12
1. `OfficialPackAcquisitionGate` — notice/workspace is not a complete pack.
2. `AttachmentRevisionManifest` — file/revision/addendum hashes and supersession lineage.
3. `SupplierFieldProvenanceGate` — insurance/tax/turnover/references/staff/safety/certifications/capacity/geography stay UNKNOWN without current source.
4. `BidHoldNoBidRouter` — no positive decision before pack + profile + requirement join.
5. `TenderFinanceNullObject` — unknown payment/retention/bond/insurance/cash timing remains null.
6. `SimilarReferenceMatrix` — only sourced supplier references can cover requirement categories.
7. `BlindPA4PacketGate` — same packet hash + independent reviewer + blindness + hidden first decision.
8. `PA4DivergenceCompiler` — compare decision, fatal gaps, missed/extra criteria without a magic similarity score.
9. `ResidualPaidJobDetector` — subtract free/native substitute coverage before claiming differentiation.
10. `AppendOnlyRefreshHistory` — idempotent snapshots preserving old states.
11. `ProofReceiptRouter` — separate PA5 real-use, E3 real external behavior, E4 real transaction receipts bound to artifact lineage.
12. `RepeatedDefectSIRouter` — one defect = discovery; repeated evidenced defect + repair = scoped candidate only.

## Contracts C7A-C01–C7A-C18
- `PUBLIC_NOTICE_NEQ_COMPLETE_OFFICIAL_PACK`.
- `NONAUTHORITATIVE_COPY_CANNOT_COMPLETE_PACK`.
- `NO_BYTES_NO_ATTACHMENT_HASH`.
- `NO_REVISION_SET_NO_SUPERSESSION_ASSERTION`.
- `SUPPLIER_FIELD_WITHOUT_PROVENANCE_IS_UNKNOWN`.
- `NO_PACK_OR_PROFILE_NO_BID_RECOMMENDATION`.
- `UNKNOWN_REQUIREMENT_ROUTES_HOLD`.
- `VERIFIED_NONCURABLE_GAP_CAN_ROUTE_NO_BID`.
- `BID_CANDIDATE_NEQ_PROCUREMENT_ELIGIBILITY_PROOF`.
- `MEAT_LABEL_NEQ_MEAT_WEIGHTS`.
- `UNKNOWN_FINANCE_TERM_NEQ_ZERO`.
- `REFERENCE_MATCH_REQUIRES_SOURCED_REFERENCE`.
- `PA4_REQUIRES_SAME_PACKET_HASH`.
- `PA4_REQUIRES_INDEPENDENCE_AND_BLINDNESS`.
- `SYNTHETIC_USER_NEQ_PA5_OR_E3`.
- `FREE_NATIVE_COVERAGE_SUBTRACTED_BEFORE_PAID_JOB`.
- `PA5_NEQ_E3_NEQ_E4`.
- `STALE_BRANCH_REBUILT_ON_FRESH_MAIN_NOT_FORCE_MERGED`.

## Proofs C7A-P01–C7A-P08
1. `CompleteOfficialPackProof`.
2. `VerifiedSupplierProfileProof`.
3. `BidRoutingFailClosedProof`.
4. `FinanceNullSafetyProof`.
5. `BlindSamePacketPA4Proof`.
6. `ResidualPaidJobProof`.
7. `RealObservationAndLineageProof`.
8. `ProofTransitionIsolationProof`.

## Protocols C7A-R01–C7A-R08
1. Authoritative Pack Acquisition.
2. Supplier Evidence Ingest + field expiry.
3. Requirement Join + BID/HOLD/NO-BID routing.
4. Blind Same-Packet PA4.
5. Real Target-User DecisionDelta.
6. PA5→E3→E4 separate receipt chain.
7. Refresh/Supersession History.
8. Fresh-Main Reconcile + GitHub/Drive readback.

## Self-Improvement disposition
The extension does not create a new global SI version. It supplies canary evidence for these bounded rules:
- missing authority is a typed state;
- analog documents cannot backfill current requirements;
- real observation cannot be synthesized;
- stale concurrent work must be reconciled, not force-merged.

Global Self-Improvement remains v2 CURRENT; v3 remains candidate.