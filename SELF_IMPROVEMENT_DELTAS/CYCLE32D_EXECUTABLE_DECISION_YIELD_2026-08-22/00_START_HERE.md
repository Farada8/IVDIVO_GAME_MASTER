# IVDIVO SELF-IMPROVEMENT — CYCLE32D EXECUTABLE DECISION-YIELD + REGISTRY-RACE GUARDS — 32→64

**Date:** 2026-08-22  
**Status:** EXECUTABLE LOCAL PILOT / 32 RUN CARDS EXECUTED / NO GLOBAL AUTHORITY PROMOTION  
**Controlling authority:** Self-Improvement v2 remains `VERIFIED_CURRENT`.  
**Cycle32C relation:** executable implementation of the highest-value unfinished Cycle32C specifications; not a replacement engine.

## Why this cycle exists
Cycle32C proved the conceptual need for a Decision/Evidence Yield control profile and a cross-branch registry collision guard, but explicitly created **no new runnable code**. Cycle32D implements the bounded mechanical subset and tests it against live current-state facts.

## Fresh inputs
- GitHub main observed advancing from `2238eb296c09...` to `4d6dc7c5dc24...`, then `ddf7864eadb4...`, then `ee49be52766e...`, and latest `acb94116576c...` during this session. `ee49be...` merged Self-Improvement Cycle9; `acb941...` added the first genuine SI-0014 recovery evidence without global promotion.
- PR #147 remains OPEN / DRAFT / NOT MERGED and carries a concurrent `SI-0016` reservation signal.
- Cycle32C Drive folder exists with 32/32 pilot artifacts; its GitHub B03 branch persistence is partial/diverged, so semantic salvage is used instead of blind merge.
- Six current mounted/uploaded artifacts are hashed and classified in `08_INPUT_ASSET_REGISTRY.*`.

## New executable modules
Registry collision guard; authority stack resolver; freshness vector; meta-WIP limiter; production-return guard; prompt capability fingerprint/dedupe; evidence-yield ledger; ordinal VOI router; qualitative Cost-of-Delay; proof/evidence classifier; external-evidence firewall; fail-closed router; observability without vanity score; knowledge compactor; selective rollback planner; promotion disposition; Next64 compiler; input-asset registry validator.

## Deterministic proof
`pytest -q test_cycle32d_engine.py` → **32 passed / 0 failed**.

## Promotion boundary
This cycle may strengthen a local v2 extension pilot. It does **not** promote v3, does not create a new CURRENT self-improvement authority, does not assign a new global SI ID, and does not manufacture Human/provider/market/payment evidence.

## Fresh rebase after Business Cycle7 and Self-Improvement Cycle9
Main first advanced to `ddf7864eadb4c3addba535690e936a77d76b0c1f`, merging Business Engineering Cycle7. It then advanced again to `ee49be52766e2f70ae010281804ffdebd6538796`, merging Self-Improvement Cycle9 real-interruption convergence.

Cycle9 already owns fresh-main gating, semantic salvage, registry-reservation law, evidence firewall, information-gain routing, WIP governance, readback and bounded closure. Cycle32D therefore **does not duplicate those laws as a new authority**. Its retained executable delta is narrower: active-PR SI-ID extraction/collision detection, multi-dimension freshness-vector calculation, prompt capability fingerprint/dedupe, evidence-yield/no-effect gates, input-asset registry validation, selective rollback helpers and a dependency-aware Next64 compiler. These act as conformance/utility code under v2/Cycle9, not as another top-level engine.
