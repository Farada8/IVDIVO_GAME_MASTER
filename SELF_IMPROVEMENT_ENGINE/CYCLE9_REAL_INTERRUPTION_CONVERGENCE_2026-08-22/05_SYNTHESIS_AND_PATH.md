# Cycle9 Synthesis — Path to Self-Improvement

## What changed
Cycle9 did not create another top-level self-improvement engine. It converted a real browser/dialog interruption into a bounded engineering/evidence cycle and reconciled it with the current verified stack.

## 32-run disposition
- 32/32 prompts executed analytically against current GitHub/Drive state.
- 32/32 deterministic Cycle9 engineering canaries pass.
- Those canaries are **engineering evidence only**.
- Real interruption observed: YES.
- Fully verified qualifying recovery event: NO / HOLD.
- SI-0014 promotion count delta: 0.
- SI-0015 remains READY_FOR_PILOT.
- Self-Improvement v3 remains CANDIDATE.
- New SI ID: NONE.

## Main conclusions
1. **Observation != qualified recovery evidence.** A browser closure is a real event, but promotion evidence starts only after project-slice recovery, readback and zero-false-resume proof.
2. **Persisted state is the continuity backbone.** Chat memory may discover leads, but authority comes from GitHub/Drive/current project sources.
3. **Source adequacy applies to recovery.** A router/summary that omits details must yield `INSUFFICIENT_SOURCE`, never invented reconstruction.
4. **Fresh-main is not optional.** Main advanced during this session. Every material write/merge needs a fresh compare and stale-work disposition.
5. **Cross-store closure must be transactional.** GitHub-only or Drive-only persistence is `PARTIAL_PERSISTENCE`.
6. **No new recovery OS.** Existing v2 + transcript recovery + SI-0014 + SI-0015 cover the semantic space; Cycle9 extends evidence and interfaces.
7. **v3 has local value but insufficient promotion evidence.** S5/S3/S2 layers help reason about this incident, but the whole v3 architecture remains candidate.
8. **Prompt count is not the goal.** The next 64 are an evidence backlog; only dependency-valid, high-information items should run.

## Engine architecture after Cycle9
`INTERRUPTION OBSERVED`
→ `AUTHORITY SNAPSHOT`
→ `PROJECT PARTITION`
→ `SOURCE ADEQUACY`
→ `FRONTIER RECONSTRUCTION`
→ `SLICE FRESHNESS / STALE-WORK GATE`
→ `FALSE-RESUME GUARD`
→ `MISSING SAFE ACTIONS`
→ `CROSS-STORE PERSISTENCE`
→ `READBACK`
→ `QUALIFICATION CHECK`
→ `LEARNING LEDGER`
→ `PROMOTION / HOLD`
→ `RETURN TO HIGHEST-INFORMATION PROJECT GATE`

## Exact path to improvement
Primary next proof is not another architecture document. It is:
1. recover one explicitly named project affected by the real browser interruption;
2. verify its exact frontier from persisted sources;
3. run stale/false-resume checks;
4. persist and read back the recovered slice;
5. only then decide whether this real incident contributes one qualifying SI-0014 event;
6. repeat on later independent interruption events until the existing promotion gate is actually met.

## Promotion decision
- Self-Improvement v2: **PRESERVE VERIFIED_CURRENT**
- SI-0014: **HOLD / collect genuine interruption-recovery evidence**
- SI-0015: **READY_FOR_PILOT / use on real recovered slices**
- v3 candidate: **HOLD CANDIDATE / bounded mechanism evaluation only**
- Cycle9 package: **BOUNDED ENGINEERING + REAL-INCIDENT OBSERVATION; NOT GLOBAL AUTHORITY**
