# CYCLE11 — NEXT 64 EVIDENCE-DRIVEN PROMPTS

**Status:** DESIGNED / NOT BLINDLY AUTO-AUTHORIZED.  
Dependencies and real evidence outrank prompt order. A genuine interruption must never be manufactured to satisfy a card.

## A — Current authority / registry / pointer convergence (01–08)
01. Reread fresh GitHub main, current Self-Improvement router, machine state, registry family and Cycle9 manifest; emit one authority/freshness vector.
02. Draft the smallest evidence-pointer refresh for `CURRENT_IVDIVO_SELF_IMPROVEMENT_STATE.json` without changing v2 authority or candidate lifecycle status.
03. Refresh the registry-family SI-0014 evidence note from pre-pilot wording to the exact current `1/3 events, 2/2 projects, zero false resume`, preserving `READY_FOR_PILOT`.
04. Refresh SI-0015 evidence note with the Business Engineering real freshness/rebase pilot while preserving remaining healthy-control/false-positive gates.
05. Recheck PR #147 before any ID operation; if SI-0016 reservation changed, recompute the complete registry-family reservation view.
06. Build a single machine-readable CURRENT Self-Improvement capability-owner map: mechanism -> owner -> status -> evidence -> next gate -> deprecation/supersession.
07. Build a deprecation/provenance map for Cycle32C, Cycle32D, parallel Cycle10 and stale/self-superseded Self-Improvement PRs; never delete unique evidence.
08. Add an AuthorityPointerLag receipt format that records old pointer, newer closure, rebase action, salvage delta and final readback.

## B — Systems source library normalization (09–16)
09. Produce a machine-readable 35-entry systems RAW manifest with Drive ID, title, MIME type, size and observed timestamp.
10. Obtain byte hashes for the 35 entries when raw download is available; leave hash null where the connector cannot prove bytes.
11. Detect exact duplicate byte groups; never infer duplicate work from similar filenames alone.
12. Quarantine placeholder/truncated files by content/size evidence, including the small SRE and converted educational-tech candidates.
13. Resolve edition/format aliases into canonical-work IDs only where evidence permits; unresolved aliases stay null.
14. Create Source Passports for the highest-decision-value systems sources: Beer, Senge, Meadows/Sterman, Reinertsen, Hubbard, SRE/Accelerate, Toyota Kata.
15. Compute mechanism-to-source concentration risk after dedupe so five copies/editions cannot masquerade as five independent sources.
16. Update `SELF_IMPROVEMENT_ENGINE/LIBRARY_CURRENT/SOURCE_REGISTRY.json` only after manifest/hash/passport readback; raw copyrighted bytes remain Drive-only.

## C — SI-0014 genuine recovery event #002 / #003 conditional path (17–24)
17. **CONDITIONAL:** when a genuinely involuntary interruption occurs, open recovery incident #002 with immutable event provenance; otherwise return `HOLD_REAL_EVENT`.
18. Recover the highest-value affected project slice for event #002 entirely from persisted sources and record unresolved chat-only material separately.
19. Run SI-0015 freshness on every recovered slice before resume; stale embedded pointer must route `REBASE_FIRST`.
20. Complete GitHub + Drive readback for event #002 and run explicit false-resume audit before counting it.
21. Increment SI-0014 only if event #002 is real, readback-complete and zero-false-resume; otherwise retain failure evidence without count inflation.
22. **CONDITIONAL:** repeat the same protocol for genuine event #003 only after such an event actually occurs.
23. After event #003, verify unique incident IDs >=3 and distinct project IDs >=2; duplicate project slices must not multiply incident count.
24. If the numerical gate is satisfied, produce a promotion-review packet only; the machine still may not self-authorize `VERIFIED_CURRENT`.

## D — SI-0015 real project-slice freshness proof (25–32)
25. Choose one healthy current project slice as a negative control where no rebase should be requested.
26. Choose one genuinely stale project slice as a positive control with a known newer authority closure.
27. Run SI-0015 on both controls and measure false-positive / false-negative outcomes rather than only PASS count.
28. Add explicit typed Founder-approval/lock-event distinction to the pilot so generic CONTINUE cannot satisfy an approval event.
29. Measure how often freshness routing changes the actual resume decision compared with a baseline that trusts embedded CURRENT labels.
30. Record manual override cases and whether the override reveals a model/rule defect or source insufficiency.
31. Test SI-0015 on one non-writing domain (audio or business) and one writing/book domain without leaking project-specific facts into the generic mechanism.
32. Reassess SI-0015: `READY_FOR_PILOT / ELIGIBLE_FOR_REVIEW / NARROW / ROLLBACK`; do not infer promotion from deterministic canaries alone.

## E — Durable persistence / recovery hardening (33–40)
33. Validate CrossStoreArtifactRecord against one real Cycle10R artifact after both GitHub and Drive writes exist.
34. Test missing semantic anchor despite both files existing; require PARTIAL/HOLD rather than persistence PASS.
35. Test correct content in the wrong Drive folder/branch; require destination mismatch failure.
36. Test empty-but-existing Drive/Google Doc and ensure it cannot satisfy readback.
37. Reproduce a safe stale-SHA GitHub write rejection or reuse a real recorded 409 case; prove no force overwrite.
38. Run an idempotent recovery from GitHub-only or Drive-only partial state and prove accepted side is not replayed.
39. Run a crash/restart recovery fixture where intent exists but action result is unknown; reversible action requires store verification before retry.
40. Produce a cross-store transaction receipt with final semantic parity, readback identities and separate external-evidence class = unchanged/null.

## F — v3 mechanism tribunal / measured downstream value (41–48)
41. Map every v3 reference-ingest mechanism to an existing v2/current owner or a precise unresolved gap.
42. Select at most one true-gap v3 mechanism for a bounded production pilot; if all are owned, return `MERGE/NO_OP`.
43. Run that mechanism prospectively on one real book-production decision with before-state, decision, overhead and rollback target recorded.
44. Run the same mechanism on one independent audio or business decision if the mechanism is genuinely portable.
45. Include a healthy no-change control and record false-positive pressure.
46. Measure downstream net gain using decision/rework/time evidence actually observed; unknown benefit/cost remains null.
47. Run regression against existing v2 owner boundaries and project-specific overlays.
48. Decide mechanism-level `MERGE_WITH_V2 / KEEP_LOCAL_PILOT / ELIGIBLE_FOR_REVIEW / HOLD / REJECT`; never promote whole v3 by package reputation.

## G — Decision/evidence yield and anti-meta-bloat (49–56)
49. Apply the parallel Cycle10 DecisionChangeContract prospectively to one active book-production meta step before work starts.
50. Apply it to one active audio-production meta step.
51. Apply it to one Business Engineering meta step.
52. For each, record whether the meta step changed a gate/decision, removed a blocker, prevented rework or produced only documents.
53. Inventory active Self-Improvement/meta WIP across GitHub and Drive; classify ACTIVE / PILOT / HOLD / STALE / DUPLICATE / PROVENANCE_ONLY.
54. Select one duplicate prompt/module/document family and compact it without losing unique evidence or recovery provenance.
55. Measure document/prompt count removed and prove capability/evidence coverage is unchanged or improved.
56. Enforce production return: if additional meta work has lower information value than current book/audio/business action, route `RETURN_TO_PRODUCTION`.

## H — Convergence / independent review / closure (57–64)
57. Reconcile open PR #211 against fresh main and Cycle10R; salvage only unique observability/promotion evidence and close/supersede if fully represented elsewhere.
58. Reconcile the parallel Cycle10 Decision/Evidence-Yield branch/package: complete missing persistence/readback or mark provenance-only; do not create a second current authority.
59. Reconcile Cycle32D PR #206: extract any still-unique verified-persistence/decision-yield delta, then close/supersede or retain HOLD with an explicit unresolved reason.
60. Refresh the Self-Improvement library index with final Cycle10R, parallel Cycle10 and provenance relationships after GitHub/Drive readback.
61. Run an independent Red Team focused on event-count inflation, registry-ID races, stale CURRENT pointers, persistence laundering, library-count laundering and meta-starvation.
62. Apply only justified current-state/pointer updates; do not change v2 authority or candidate lifecycle without meeting their exact gates.
63. Perform final GitHub main + PR merge-ref CI + Drive content readback + semantic-anchor cross-store closure and record exact identities.
64. Choose the next single highest-information real action. Do **not** automatically produce another 128-card cycle; if a real event/provider/human/market/production gate is now more informative, stop meta-work and execute that gate.