# CURRENT NUMERICAL-ORDER / SYNTHESIS RESEARCH NOTE — 2026-08-22

**Status:** CURRENT RECOVERY AUTHORITY / ENGINEERING-MATHEMATICAL WORKING RESULT  
**Evidence ceiling:** no novelty claim; no claim of new theorem or new physics.  
**Drive source authority:** `SYNTHESIS.md` — Drive ID `1aLjhfQGupMOWmtYgeuqwFInfzhwqbBgL`  
**Recovery purpose:** make the newest research frontier recoverable without source chat.

## Non-negotiable methodological constraints

1. `NUMBER != NUMERAL_REPRESENTATION`.
2. The proposed 16-system / 16-based architecture remains a **search hypothesis**, not an established fact.
3. A synthesis that re-enters a higher tier as a new element remains meaningful only after its domain, operator, embedding/re-entry map, symmetries, invariants, interventions and counterexamples are defined.
4. Compression/projection must be tested on hidden tasks and interventions; train-fit alone is not evidence of sufficiency.

## Current CYCLE2 result

A lossless "absolute projection" does **not** create new information. It may still define a useful new working tier when it preserves all declared future tasks/interventions while reducing representation or construction cost.

The most precise current object is:

`MINIMAL SUFFICIENT INVARIANTS relative to (G, Tasks, Interventions)`

where `G` is the declared symmetry group.

### Benchmarks recovered from current Drive source

- Fully symmetric task: `VIETA` is essentially exact on train and hidden data (~1e-13 RMSE); SUM/FULL/BLOCK_VIETA are not.
- Fully role-sensitive task: `FULL` is essentially exact (~1e-13 RMSE); VIETA/SUM/BLOCK_VIETA fail at ~1.76–1.80 RMSE.
- Block-symmetric task: `BLOCK_VIETA` is essentially exact (~1e-13 RMSE); the alternatives fail materially.

Interpretation: the sufficient representation depends on the symmetry and the future task/intervention class. There is no universally valid scalar compression merely because a compact coordinate system exists.

## Viète dynamics fixture

For `V(x,y)=(x+y,xy)`, the full line `(x,0)` is fixed. The transverse multiplier is `x`; therefore `|x|<1` is locally attracting normal to the fixed line and `|x|>1` is repelling. Recursion may stabilize or diverge without adding exogenous information.

## False-compression control

A co-moving training manifold can make a role-sensitive target appear nearly determined by SUM. Hidden independent-role tests expose the failure and require FULL. This is the current adversarial fixture against selecting a projection only because it is small and fits training data.

## Next research target

Declare or learn a subgroup `G <= S_n`, then search for the smallest `G`-invariant projection sufficient for the declared tasks and interventions. Test:

- hidden role perturbations;
- intervention preservation;
- permutation/block-permutation invariance;
- counterexamples where compressed coordinates collide but required futures differ;
- construction/representation cost versus retained predictive/control sufficiency.

## Recovery disposition of older chat hypotheses

- synthesis -> new element -> re-entry: **PRESERVED AS HYPOTHESIS, now constrained by sufficiency/invariance tests**;
- tier/order iteration: **PRESERVED AS HYPOTHESIS, not automatically information-generating**;
- 16-system architecture: **OPTION / SEARCH HYPOTHESIS**;
- claims that numeral-base change itself creates a new mathematical order: **REJECTED AS METHODOLOGICAL CONFUSION**.

`MATH_DELETE_GATE = SAFE_FOR_CURRENT_RESEARCH_AUTHORITY`

`MATH-CURRENT-20260822-MIN-SUFFICIENT-INVARIANTS-G-TASKS-INTERVENTIONS-NO-NOVELTY-CLAIM`
