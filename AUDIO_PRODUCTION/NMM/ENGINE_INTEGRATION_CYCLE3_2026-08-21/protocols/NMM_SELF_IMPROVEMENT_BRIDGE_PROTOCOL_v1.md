# NMM SELF-IMPROVEMENT BRIDGE PROTOCOL v1

1. Capture `DEFECT -> ROOT CAUSE -> REPAIR -> RETEST -> RESULT`.
2. Bind source/build/evidence hashes.
3. Store NMM production record in universal learning-registry-compatible shape.
4. Strip names, exact clue chains and asset identities before proposing reusable mechanism.
5. One-project evidence = `DISCOVERY_ONLY`.
6. Cross-project replication + required human evidence may become `CANDIDATE_FOR_REVIEW`.
7. No automatic CURRENT/canon/runtime promotion.
