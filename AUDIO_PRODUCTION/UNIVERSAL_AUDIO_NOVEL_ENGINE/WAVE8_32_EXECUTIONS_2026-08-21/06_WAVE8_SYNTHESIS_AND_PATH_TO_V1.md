# WAVE8 — SYNTHESIS AND PATH TO V1

## Decision
The engine is not blocked by another architecture problem. Wave8 01–08 reconfirms a strong internal control plane and converts the remaining uncertainty into explicit evidence contracts. The first non-paper uncertainty is authenticated provider reality.

Current state:
`INTERNAL_CONTROL_PLANE_CONVERGED / ARCHITECTURE_REFROZEN / FRESH_CI_EVIDENCE_DEBT / AUTHENTICATED_PROVIDER_GATE_OPEN / V1_HOLD`.

## What Wave8 adds
1. A machine-readable negative-fixture catalog for byte-touch/provider boundaries.
2. ProviderSnapshotContract: secret-free authenticated inventory as the dispatch authority.
3. HumanEvidenceLockContract: heard evidence cannot be replaced by model judgment.
4. LiveEscrowContract: paid requests become restart-safe auditable lineages.
5. ArchitectureRefreezeProtocol: no generic module proliferation without demonstrated defect.
6. SelfImprovementEvidenceLoop: earliest-failure-layer patching + rollback + independent replication.
7. Baseline-first spending protocol.

## Strongest finding
Current controlled dispatch already enforces the most dangerous live properties:
- dry is default;
- new live spend requires identity + authenticated capability evidence;
- missing voice blocks instead of auto-swapping;
- accepted request hashes are not resent after restart;
- ambiguous post state is quarantined;
- provider acceptance is not take lock.

Therefore the next useful engineering action is not “improve dispatch.” It is to obtain the authenticated snapshot that the dispatch already requires.

## Evidence debt
Fresh full CI for the Wave7 merge/current main was not executable in this environment and no attached GitHub Actions run was found. Prior PR103 evidence remains useful but is not relabeled as a fresh Wave8 run.

## External gates, in order
1. authenticated secret-free ElevenLabs snapshot;
2. current model/capability proof;
3. real Narrator/Ethan/Aoife candidates;
4. human-heard Ифа/Контакт;
5. state/pair/fatigue human evidence;
6. full pre-spend GO;
7. exactly RB001 -> human stop -> RB002 -> human stop -> RB003;
8. durable live escrow/readback;
9. canonical 48k + 36/36 alignment;
10. sample-accurate cue timeline;
11. sparse mix + blind human benchmark;
12. measured cost/human time;
13. second materially different live project;
14. independent v1 red team.

## Stop rule
If authenticated provider access is unavailable, do not generate more “architecture” to compensate. Persist the blocked condition and route the next experiment to the access/provider boundary.
