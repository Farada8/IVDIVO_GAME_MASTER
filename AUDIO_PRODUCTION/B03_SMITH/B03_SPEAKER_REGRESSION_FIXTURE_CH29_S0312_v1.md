# B03 speaker regression — CH29 S0312

Expected acoustic speaker: `OES_SUPERVISOR_CH29`.

Failure pattern: male-pronoun resolver chose the earlier named male Smith instead of the active local subject.

Scene anchor: the OES supervisor has just entered the room; narration says the supervisor joined five minutes later and `He had not slept either.` The subsequent `I have the case instruction,` followed by `he said` belongs to that supervisor.

Mandatory regression: a resolver must not promote `SMITH` for `B03_CH29_S0312` solely because Smith is the nearest earlier named male.

Drive mirror: `1R8tTyvK4mkxfmklCe9PoZzTJPg_RXYmk`.
