# ROOM 917 — POST-RENDER 32×64 HANDOFF

## Completed in this package

- 32/32 post-render prompts executed sequentially at artifact/evidence level.
- Results classified without inventing missing listening/provider evidence.
- Current GitHub project state rebased to v3.3.
- Integrated synthesis/Red Team/GO-NO-GO completed.
- Exactly 64 next prompts derived and dependency ordered.
- Google Drive folder created/used and current docs moved into it.
- GitHub package created on a clean branch from current main because the previously named branch had unrelated concurrent work.

## Protected current truths

- Locked story remains untouched.
- Real E01 master identity remains immutable.
- P003A complete.
- D003 aggregate pre-Scene3 room-bed undercoverage is proven MAJOR but exact repair intervals are unresolved.
- D004 remains candidate; D005 remains hypothesis.
- Scene3 v1.3E technical lineage remains protected.
- No blanket ambience/music/SFX repair.
- No E02–E24 scale before E01 v2 proof.

## Exact current next obligation

`P003A2_PRE_SCENE3_EXACT_INTERVAL_LOCALIZATION`.

Required evidence path:
1. materialize the exact immutable master bytes, or locate a persisted exact interval map;
2. extract contiguous pre-Scene3 intervals below -85/-50/-45 dBFS on the recorded 100 ms basis;
3. map each interval to Scene1/2 lineage;
4. classify against the current interval classification law;
5. authorize only demonstrated D003 room/ambience support repairs.

Parallel real gate: `P003B_HUMAN_PERCEPTUAL_LISTEN`.

## Stop condition

Current tool surface does not expose the immutable master bytes and no exact interval map has been recovered. Do not simulate P003A2/P003B. Resume automatically when either evidence path becomes available.

## After evidence arrives

`P003A2 -> P003B -> issue-specific P004 -> A_FORENSIC_CONTROL / B_COMMERCIAL / C_PREMIUM -> loudness-matched Founder/blind comparison -> economics -> E01 v2 GO/NO-GO -> limited season canaries -> controlled scale -> second-project portability -> universal engine release gate`.

## Reusable learning

The studio should not respond to post-render dissatisfaction by adding architecture, prompts, music or SFX indiscriminately. The reusable loop is:

`PRODUCT SIGNAL -> CORRECT EVIDENCE CLASS -> TIMESTAMP/OBJECT -> EARLIEST FAILED LAYER -> SMALLEST PATCH -> CONTROLLED COMPARISON -> HUMAN RESULT -> ECONOMIC RESULT -> PROMOTE ONLY RECURRING PROJECT-NEUTRAL WIN`.
