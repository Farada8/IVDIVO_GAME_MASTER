# ROOM917 / IVDIVO — NEXT EXECUTION ROADMAP AFTER MC64 -> MC128 v4

## Complete
- MC64 v3: 64/64 sequentially executed on locked E02 in zero-cost planning/verification mode.
- Cross-prompt synthesis complete.
- 128-prompt v4 candidate designed.
- E02 story text unchanged.
- ROOM917 current production state unchanged.

## Track 1 — current real ROOM917 production (higher priority)
Do not use E02 research to jump over the current E01 frontier.

Authoritative next action remains `P003A2_PRE_SCENE3_INTERVAL_LOCALIZATION` when exact immutable master bytes or a persisted interval map become accessible.

Then:
1. extract exact low-level intervals;
2. classify protected silence vs valid low density vs missing room support vs causal-Foley candidate;
3. prove exact D003 patch intervals;
4. test D004 causal-Foley underdensity rather than assume it;
5. create selective room/Foley patch contracts only for proven intervals;
6. preserve accepted Scene3 lineage;
7. run real Founder/human perceptual gate;
8. if justified, build A forensic / B commercial / C premium variants;
9. decide scale GO/NO-GO.

## Track 2 — v4 research validation
1. run MC-000 lifecycle router first;
2. activate only phase-relevant families in normal production;
3. keep one complete sequential benchmark for research comparison;
4. use E02 as pre-render/scene-embodiment case;
5. use E01 immutable master as post-render forensic case when bytes materialize;
6. use a second materially different scene/project for generality;
7. compare time, output quality and redundancy vs v3;
8. remove prompts that do not change decisions;
9. promote only validated mechanisms.

## Hard prohibitions
- no E02 paid render before E01 scale gate;
- no 128-prompt mechanical ritual as normal production;
- no fake E02 absolute timings;
- no invented fourth-note pitches;
- no simulated human-listener PASS;
- no blanket fill of E01 low-level intervals;
- no music substitution for missing room bed/Foley;
- no locked-story rewrite to satisfy tooling;
- no overwrite of newer concurrent state without rebase.

## Exact research handoff
`MC64_VALIDATED_ON_E02_ZERO_COST -> MC128_V4_WORKING_CANDIDATE`.

The next experimental test is **lifecycle-routing + dedupe + real post-render interval forensics**, not immediate further prompt-count inflation.