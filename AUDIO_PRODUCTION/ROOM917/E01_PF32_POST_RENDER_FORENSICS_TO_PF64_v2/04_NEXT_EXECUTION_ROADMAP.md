# NEXT EXECUTION ROADMAP — PF64

## Phase 0 — complete now
PF32 has already closed all zero-cost planning work that does not require WAV bytes or human perception: authority/evidence classes, extraction schema, interval taxonomy, room/Foley/music firewalls, patch/regression templates and A/B/C design.

## Phase 1 — when exact master bytes OR exact interval map becomes accessible
Run PF-000…PF-031 as needed for:
1. immutable master identity/parity;
2. exact 100 ms low-level interval extraction;
3. scene/cue lineage join;
4. interval classification;
5. D003 selective room-bed patch contracts.

Do not run music enhancement first.

## Phase 2 — after classified D003 intervals
- patch only `MISSING_ROOM_OR_AMBIENCE_SUPPORT`;
- preserve `PROTECTED_AUTHORED_PAUSE` and `VALID_LOW_DENSITY`;
- leave `UNKNOWN` untouched pending listen;
- remeasure and run regression.

## Phase 3 — D004 evidence
Use live audio/cue evidence to verify obligated physical actions. Promote D004 only where missing/under-mixed action is demonstrated.

## Phase 4 — human/commercial
After physical-world correction:
- P003B human listen;
- loudness-matched A/B/C;
- one-listen comprehension;
- relationship/false-intimacy check;
- phone/mono/headphone/1.25x translation.

## Phase 5 — E01 V2 gate
Only on PASS:
- authorize scale candidate for E02;
- test PF64 on a second project/scene;
- semantic-dedupe and promote only mechanisms that materially improve decisions.

## Current exact blocker
`MASTER_BYTES_OR_EXACT_INTERVAL_MAP`.

## Current exact next production action
Materialize the immutable E01 master or import a trusted exact interval map; then execute P003A2 interval localization. No additional planning prompt can substitute for this evidence.
