# STORY MECHANISM INTELLIGENCE — LOCAL PROOF + B03 SHADOW CONTROL

## Deterministic runtime regression
Command: `pytest -q test_story_mechanism_intelligence.py`  
Result: **24 passed / 0 failed**.

Covered invariants include relevance ordering, max-three cap, hard contraindications, HOLD/REJECT exclusion, originality firewall, project-only portability, prerequisites, forbidden moves, explicit pair incompatibility, complementary effect coverage, locked SHADOW_ONLY behavior, duplicate source-group non-inflation, prediction-vs-observation labeling, baseline evidence hold, protected/MAJOR regression blocking, one-project/two-project promotion readiness, FATAL application hold and source-claim immutability.

## B03 locked shadow fixture
Current B03 authority is Founder-locked CH01–29 and release-ready. The fixture therefore sets `locked=true` and `stage=LOCKED_SHADOW`.

Candidate inputs:
- M010 Waiting-Question Ledger — non-mutating diagnostic;
- M011 Scene-Question / Outcome-Integrity Gate — non-mutating diagnostic;
- a synthetic mutating mechanism attempting locked-story rewrite.

Observed engine behavior:
- output status = `SHADOW_ONLY`;
- non-mutating current craft mechanisms may be selected for retrospective analysis;
- the mutating mechanism is ineligible under `locked_text_mutation_forbidden`;
- packet includes `LOCKED_TEXT_SHADOW_EVALUATION_ONLY_NO_MUTATION`;
- predicted effects remain marked `PREDICTION_ONLY_NOT_OBSERVED_RESULT`.

This is an engineering lock-control proof, not evidence that B03 should be revised and not a literary-quality A/B result.
