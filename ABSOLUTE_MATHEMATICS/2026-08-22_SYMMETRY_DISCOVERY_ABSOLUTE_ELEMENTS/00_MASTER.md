# SYMMETRY DISCOVERY → ABSOLUTE ELEMENT GENESIS

## Current result

For four peers, the same 15 formal nonempty relations can collapse to different numbers of structural relation types depending on the discovered/declared symmetry group:

- full interchangeability `S4` → 4 relation types;
- two independent interchangeable blocks `S2 x S2` → 8 relation types;
- cyclic rotation `C4` → 5 relation types;
- unique roles → 15 relation types.

The underlying scalar state can still have only four independent degrees of freedom.

Therefore:

`formal relation count != symmetry relation-type count != independent state dimension`.

## Minimal task-sufficient absolute elements

| Regime | Formal relations | Relation types | Minimal features for tested task | Universal/state hint |
|---|---:|---:|---:|---:|
| S4 symmetric | 15 | 4 | 2 | 4 elementary symmetric generators |
| S2xS2 blocks | 15 | 8 | 4 | 4 block sum/product generators |
| Unique roles | 15 | 15 | 4 | 4 labeled scalar coordinates |
| D4 cycle task | 15 | 5 | 1 | not derived generically here |

Adding a second S4-symmetric task that requires triple interactions raises the common minimum from 2 to 3.

## Revised definition

`AbsoluteElements = MinimalSufficientInvariants(G, Tasks, Interventions, promotion_mode)`.

Two promotion modes are now mandatory:

- `UNIVERSAL`: preserve all distinctions needed to reconstruct the declared semantic quotient;
- `TASK`: preserve only preregistered tasks/interventions.

A task-specific projection can be smaller but becomes invalid if the task/intervention family expands.

## Claim ceiling

Underlying orbit counting, invariant theory, symmetric-polynomial representation and permutation-invariant set representation are established mathematics. Current project status is:

`ENGINEERING_SYNTHESIS / NOVELTY_UNVERIFIED`.

No new physical dimension, ontology, or universal mathematical law is claimed from the relation-count growth alone.
