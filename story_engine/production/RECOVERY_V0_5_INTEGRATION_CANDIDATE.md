# IVDIVO IDEA→STORY — RECOVERY v0.5 INTEGRATION CANDIDATE

**Status:** WORKING / ENGINEERING UPGRADE CANDIDATE — NOT CURRENT ENGINE AUTHORITY  
**Recorded:** 2026-08-21

## Authority boundary

The accepted executable IDEA→STORY v1.0.0 remains current execution authority until an explicit promotion replaces it.

Recovery v0.5 is a clean-room WORKING engineering baseline. Verified recovery artifacts report:
- `130/130` automated tests PASS;
- `4/4` production smoke scenarios PASS;
- status `GO_RECOVERY_ENGINEERING_BASELINE__NO_GO_RELEASE_READY_ENGINE`.

Engineering tests do not prove literary quality, live-provider calibration, market performance or exact equivalence to the missing historical v1.0.0 source tree.

## Port-worthy verified mechanisms

- global earliest-layer resolution;
- `STAGE_REGEN_REQUEST`;
- `STAGE_REPAIR_CONTRACT`;
- semantic candidate delta before commit;
- separate repair QA;
- transaction snapshot + rollback;
- true dependency/descendant rebuild;
- selective Architecture scene redraft at scene-card hash boundaries;
- global-before-local repair ordering;
- Founder reapproval boundary after automatic repair of Founder-approved CANON;
- Character identity-set protection;
- Relationship candidate protection against unknown character IDs;
- semantic no-effect rejection;
- corrected manual `--regen-stage` descendant invalidation;
- inherited local path: `PATCH_QUEUE_STATE -> PATCH_CONTRACT -> candidate -> local Patch QA -> target commit -> selective regression`.

## Smoke evidence

- `global_then_local`: upstream Architecture repair followed by local scene repair reaches review-pending draft state.
- `canon_reapproval`: automatic repair stops at authority/specialized-stage boundary rather than silently mutating approved CANON.
- `transaction_rollback`: failed downstream validation restores prior Architecture state.
- `selective_sibling_stability`: unaffected scene/draft hashes remain stable while true descendants rebuild.

## Auto-supported global stages reported by v0.5

- `STORY_CORE`
- `CHARACTER_ENGINE`
- `RELATIONSHIP_AUTHORITY`
- `ARCHITECTURE`

This is evidence about the recovery baseline, not permission to mutate those layers in a current canonical run.

## Protected / non-auto boundaries

- `FOUNDER_RAW`
- approved `CANON` without Founder reapproval
- `STORY_DISCOVERY` / engine recompetition
- `WORLD`
- `MYSTERY_ARCHITECTURE`
- protected `CLUE_ORDER`
- `IDENTITY_MIGRATION`

## Open MAJOR blockers before promotion

1. Story Discovery / engine recompetition repair is not executable end-to-end.
2. Specialized World / Mystery / protected clue-order repair is not executable end-to-end.
3. Relationship Authority is not yet a full ensemble graph with edge-specific provenance/invalidation.
4. Full Story Engine v4.1 Reference Intelligence runtime (Passports / Mechanism Bank / Crosswalk execution) is not integrated.
5. `LIVE_CALIBRATION_01` has not been run with a real Founder idea and real literary/review provider bridge.
6. Human P51 blind-swap and Human Signal remain external-only evidence and must not be model-simulated.

## Canonical integration target

`DETECT DEFECT -> LOCATE EARLIEST FAILED LAYER -> FREEZE AUTHORITY + UNAFFECTED ARTIFACTS -> BUILD REPAIR CONTRACT -> SNAPSHOT -> GENERATE CANDIDATE -> SEMANTIC DELTA -> TARGET/STAGE QA -> REJECT NO-EFFECT/OUT-OF-SCOPE -> COMMIT -> INVALIDATE TRUE DESCENDANTS ONLY -> SELECTIVE REBUILD -> SELECTIVE REGRESSION -> ROLLBACK WHOLE TRANSACTION ON FAILURE -> RELOAD CURRENT DOWNSTREAM STATE -> FOUNDER REAPPROVAL IF CANON EFFECT CHANGED -> UPDATE PROJECT STATE`.

This extends the canonical earliest-failure/minimum-repair law; it does not create a parallel story system.

## Promotion gates

Before v0.5-derived mechanics can become current engine authority:
- reconcile/import actual recovery source/provenance;
- map and reproduce or explicitly supersede all 130 reported tests plus four smoke scenarios in canonical development;
- preserve current v1.0.0 source-integrity, resume/selective-regeneration and fail-closed behavior;
- pass dependency invalidation, rollback, CANON reapproval, identity protection and no-effect rejection tests;
- implement or explicitly gate Story Discovery, World, Mystery/clue and ensemble Relationship repair boundaries;
- connect full v4.1 Reference Intelligence runtime rather than replacing it with the reduced recovery adapter;
- run `LIVE_CALIBRATION_01`;
- preserve Human Signal / human blind-swap as external-only evidence;
- produce a new integration acceptance report;
- explicitly update current engine authority only after promotion decision.

## Exact next engine action

Reconcile/import Recovery v0.5 into a canonical development branch; create a test/provenance crosswalk for all 130 tests and four smoke scenarios; then close the Story Discovery + World/Mystery + ensemble Relationship + full Reference Intelligence runtime boundaries before any authority-promotion proposal.

**DO NOT PROMOTE v0.5 BY VERSION NUMBER OR TEST COUNT ALONE.**
