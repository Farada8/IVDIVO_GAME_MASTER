# IVDIVO IDEA→STORY — RECOVERY v0.5 INTEGRATION CANDIDATE

**Status:** WORKING / UPGRADE CANDIDATE — NOT CURRENT ENGINE AUTHORITY  
**Recorded:** 2026-08-21  
**Purpose:** prevent proven recovery engineering from being lost across conversations while preventing accidental canon promotion.

## AUTHORITY BOUNDARY

Current executable IDEA→STORY authority remains the accepted v1.0.0 core unless a newer authority is explicitly promoted.

Recovery v0.5 is a WORKING engineering baseline recovered from the current project/File Library evidence. Its reported acceptance suite is **130/130 PASS**, but test count alone does not make it higher authority.

Before promotion, source package/provenance must be imported or reconciled into the canonical repo and the integration gates below must pass.

## PORT-WORTHY MECHANISMS

The following mechanisms are approved for **integration evaluation**, not automatic merge into canon:

1. local `PATCH_QUEUE_STATE -> PATCH_CONTRACT -> candidate -> local Patch QA -> target-only commit -> selective regression`;
2. earliest global failed-layer resolution;
3. `STAGE_REGEN_REQUEST`;
4. `STAGE_REPAIR_CONTRACT`;
5. semantic candidate delta before commit;
6. separate stage-repair QA;
7. transaction snapshot before mutation;
8. rollback before/after failed commit path as appropriate;
9. true dependency/descendant rebuild rather than broad restart;
10. selective architecture scene redraft;
11. global-before-local repair ordering;
12. Founder reapproval boundary for CANON mutation;
13. character identity-set protection;
14. relationship unknown-character protection;
15. no-effect / out-of-scope repair rejection;
16. corrected manual `regen-stage` behavior against the descendant graph.

## SUPPORTED GLOBAL REPAIR LAYERS REPORTED BY v0.5

- `STORY_CORE`
- `CHARACTER_ENGINE`
- `RELATIONSHIP_AUTHORITY`
- `ARCHITECTURE`

Treat these as implementation evidence to be reconciled, not as an assertion that every project has been live-calibrated.

## NON-AUTOMATIC / PROTECTED BOUNDARIES

Do not auto-mutate:

- `FOUNDER_RAW`;
- `CANON` without Founder reapproval;
- Story Discovery / engine recompetition;
- specialized World repair;
- specialized Mystery architecture;
- protected clue order;
- identity migrations requiring explicit authority.

## OPEN MAJOR BLOCKERS BEFORE PROMOTION

1. Story Discovery / engine-recompetition repair is not yet executable end to end.
2. Specialized World/Mystery/protected clue-order repair is not yet executable end to end.
3. Full ensemble Relationship Authority Graph remains incomplete.
4. Full Story Engine v4.1 Reference Intelligence runtime — library passports/mechanism bank/crosswalk execution — is not yet integrated as a complete runtime path.
5. `LIVE_CALIBRATION_01` has not been proven by real provider/live execution evidence.
6. P51 blind-swap and Human Signal remain external evidence gates where required; do not synthesize them as passed.

## INTEGRATION TARGET

Port the transactional mechanics into the current engine without changing story authority semantics:

`DETECT DEFECT -> LOCATE EARLIEST FAILED LAYER -> FREEZE UNAFFECTED AUTHORITY -> BUILD REPAIR CONTRACT -> SNAPSHOT -> GENERATE CANDIDATE -> SEMANTIC DELTA -> LOCAL QA -> REJECT NO-EFFECT/OUT-OF-SCOPE -> COMMIT -> INVALIDATE TRUE DESCENDANTS -> SELECTIVE REBUILD -> SELECTIVE REGRESSION -> ROLLBACK ON FAILURE -> UPDATE STATE`.

## REQUIRED PROMOTION GATES

Before v0.5-derived mechanics become current engine authority:

- provenance/source import verified;
- no regression of current v1.0.0 accepted behavior;
- authority hierarchy preserved;
- resume/selective-regeneration behavior preserved;
- dependency invalidation tests PASS;
- transaction rollback tests PASS;
- CANON reapproval boundary tests PASS;
- identity/relationship protection tests PASS;
- no-effect rejection tests PASS;
- existing v0.5 suite reproduced in canonical environment or explicitly mapped to equivalent tests;
- new integration acceptance report produced;
- current engine authority/router explicitly updated.

## CRAFT ALIGNMENT

This integration follows the studio’s story-first repair law: line-level symptoms often originate upstream in event/character/scene design; structural revision changes dependencies downstream. Therefore the engine should repair the earliest causal layer and rebuild only affected descendants rather than repeatedly paraphrasing prose.

## NEXT ENGINE ACTION

Reconcile/import the v0.5 recovery source package into the canonical development branch, map all 130 reported tests to canonical test IDs, then implement the missing Story Discovery + World/Mystery/Relationship/Reference-Intelligence boundaries before considering authority promotion.
