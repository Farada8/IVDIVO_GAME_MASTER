# IVDIVO IDEA→STORY — RECOVERY v0.5 INTEGRATION CANDIDATE

**Status:** WORKING / ENGINEERING UPGRADE CANDIDATE — NOT CURRENT ENGINE AUTHORITY  
**Recorded:** 2026-08-21  
**Scope:** IDEA→STORY repair/recovery runtime only.  
**Purpose:** preserve verified Recovery v0.5 engineering progress across conversations/models without silently replacing the current accepted IDEA→STORY authority.

## 1. AUTHORITY BOUNDARY

The accepted executable IDEA→STORY v1.0.0 remains the current execution authority until an explicit promotion decision replaces it.

Recovery v0.5 is a **WORKING clean-room recovery / engineering baseline**. Current evidence reports:
- **130/130 automated tests PASS**;
- **4/4 production smoke scenarios PASS**;
- status: `GO_RECOVERY_ENGINEERING_BASELINE__NO_GO_RELEASE_READY_ENGINE`.

Passing engineering tests does not prove literary quality, provider calibration, market performance or equivalence to the missing historical v1.0.0 source tree.

## 2. VERIFIED v0.5 ENGINEERING CAPABILITIES TO EVALUATE FOR PORTING

1. global earliest-layer defect resolution;
2. `STAGE_REGEN_REQUEST`;
3. `STAGE_REPAIR_CONTRACT`;
4. semantic candidate delta before commit;
5. separate stage-repair QA;
6. transaction snapshot before mutation;
7. transaction rollback on failed downstream gates;
8. true dependency/descendant rebuild rather than broad restart;
9. selective Architecture scene redraft using scene-card hash boundaries;
10. global-before-local repair ordering;
11. Founder reapproval boundary after automatic repair of Founder-approved CANON;
12. Character identity-set protection during automatic repair;
13. Relationship candidate protection against unknown character IDs;
14. no-effect semantic repair rejection;
15. corrected manual `--regen-stage` descendant invalidation;
16. local repair path inherited from v0.4: `PATCH_QUEUE_STATE -> PATCH_CONTRACT -> candidate -> local Patch QA -> target commit -> selective regression`.

## 3. VERIFIED TRANSACTION / SMOKE BEHAVIOR

Current v0.5 smoke evidence includes:

- `global_then_local`: upstream Architecture repair followed by local scene repair reaches `DRAFT_COMPLETE_REVIEW_PENDING`;
- `canon_reapproval`: automatic repair correctly stops at authority/specialized-stage boundary rather than mutating approved CANON silently;
- `transaction_rollback`: failed downstream validation restores prior Architecture state;
- `selective_sibling_stability`: unaffected scene/draft hashes remain stable while true descendants rebuild.

These are implementation signals worth integrating into the canonical development runtime after provenance reconciliation.

## 4. AUTO-SUPPORTED GLOBAL STAGES IN v0.5

Reported automatic upstream repair support:
- `STORY_CORE`;
- `CHARACTER_ENGINE`;
- `RELATIONSHIP_AUTHORITY`;
- `ARCHITECTURE`.

This list describes the Recovery v0.5 baseline. It does not grant automatic authority to mutate those layers in a current canonical production run.

## 5. NON-AUTOMATIC / PROTECTED BOUNDARIES

Recovery v0.5 correctly refuses generic automatic mutation of:
- `FOUNDER_RAW`;
- approved `CANON` without Founder reapproval;
- `STORY_DISCOVERY` / engine recompetition;
- `WORLD`;
- `MYSTERY_ARCHITECTURE`;
- protected `CLUE_ORDER`;
- `IDENTITY_MIGRATION`.

Do not weaken these fail-closed boundaries merely to make the recovery loop appear complete.

## 6. OPEN MAJOR BLOCKERS BEFORE PROMOTION

1. **Story Discovery / engine recompetition repair is not executable.** If premise/engine selection itself is wrong, v0.5 can detect the boundary but cannot yet run a proper rediscovery transaction.
2. **Specialized World / Mystery / protected clue-order repair is not executable.** These require authority-aware specialist contracts.
3. **Relationship Authority remains incomplete as a general ensemble graph.** Edge-specific provenance/invalidation for family, ensemble and sequel relationships is still needed.
4. **Full Story Engine v4.1 Reference Intelligence runtime is not integrated.** The recovery package does not reproduce the complete Passport / Mechanism Bank / Crosswalk execution path.
5. **`LIVE_CALIBRATION_01` has not been run.** 130 automated tests are engineering evidence, not evidence that a real Founder idea plus real literary/review providers reliably yields strong story.
6. **External evidence remains external.** Human P51 blind-swap and Human Signal gates must not be simulated by the engine/model.

## 7. CANONICAL INTEGRATION TARGET

Desired repair transaction:

`DETECT DEFECT`
→ `LOCATE EARLIEST FAILED LAYER`
→ `FREEZE AUTHORITY + UNAFFECTED ARTIFACTS`
→ `BUILD REPAIR CONTRACT`
→ `SNAPSHOT TRANSACTION`
→ `GENERATE CANDIDATE`
→ `SEMANTIC DELTA`
→ `TARGET/STAGE QA`
→ `REJECT NO-EFFECT / OUT-OF-SCOPE`
→ `COMMIT CANDIDATE`
→ `INVALIDATE TRUE DESCENDANTS ONLY`
→ `SELECTIVE REBUILD`
→ `SELECTIVE REGRESSION`
→ `ROLLBACK WHOLE TRANSACTION ON FAILURE`
→ `RELOAD CURRENT DOWNSTREAM STATE`
→ `FOUNDER REAPPROVAL IF CANON EFFECT CHANGED`
→ `UPDATE CURRENT PROJECT STATE`.

This implements the existing IVDIVO earliest-failure/minimum-repair law instead of creating a competing story system.

## 8. REQUIRED PROMOTION GATES

Before v0.5-derived mechanics can become current engine authority:

- import/reconcile the actual recovery source package and provenance;
- map the reported 130 tests to canonical test IDs and reproduce or explicitly supersede them in the canonical development environment;
- preserve all current v1.0.0 acceptance behavior, including source integrity, resume/selective regeneration and fail-closed gates;
- pass dependency invalidation tests;
- pass transaction rollback tests;
- pass CANON reapproval-boundary tests;
- pass Character/Relationship identity-protection tests;
- pass semantic no-effect rejection tests;
- implement or explicitly gate Story Discovery, World, Mystery/clue and ensemble Relationship repair boundaries;
- connect full v4.1 Reference Intelligence runtime rather than replacing it with the reduced recovery adapter;
- run `LIVE_CALIBRATION_01` with a real Founder idea and real provider bridge;
- preserve real Human Signal / human blind-swap as external-only evidence;
- produce a new integration acceptance report;
- explicitly update the current engine authority/router only after the promotion decision.

## 9. STORY/CRAFT ALIGNMENT

The recovery architecture matches current Narrative OS law:

`SYMPTOM -> ROOT CAUSE -> EARLIEST FAILED LAYER -> SMALLEST EFFECTIVE REPAIR -> SELECTIVE DESCENDANT REGRESSION`.

It also matches the studio’s craft evidence: a dialogue symptom may originate in event/character/scene design, and a structural revision propagates through dependent choices. Therefore the system must not endlessly paraphrase prose around an upstream story defect.

## 10. EXACT NEXT ENGINE ACTION

Reconcile/import the Recovery v0.5 package into a canonical development branch; build a test/provenance crosswalk for all 130 reported tests and four smoke scenarios; then close the missing Story Discovery + World/Mystery + ensemble Relationship + full Reference Intelligence runtime boundaries before any authority-promotion proposal.

**DO NOT PROMOTE THIS FILE OR v0.5 BY VERSION NUMBER ALONE.**
